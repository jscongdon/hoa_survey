# Production Environment Variables for Portainer Stack
# Most configuration is now done via the setup wizard on first run

DATABASE_URL=file:/data/hoa-survey.db

DEVELOPMENT_URL=http://localhost:3000
PRODUCTION_URL=https://yourdomain.com
NODE_ENV=production

# Note: SMTP settings, HOA branding, and JWT secret are configured
# via the first-time setup wizard and stored in the database
